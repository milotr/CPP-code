template <typename B> //T is a name, it could be anything (rule: captial letter + (numbers))

B average(B value1, B value2, B value3)
{
    B averageValue = (value1 + value2 + value3) / 3;
    return averageValue;
}
